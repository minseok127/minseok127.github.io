---
layout: default
title: 수직인 벡터들을 모으면 선형독립인가?
parent: 선형대수
nav_order: 22
---

# 수직인 벡터들을 모으면 선형독립인가?

---

### 질문

- 수직인 벡터들을 모은 집합은 선형독립인가?
- 수직인 벡터들을 $\dim S$개 모으면 $S$의 basis인가?

---

$\set{\vec{w_1}, \vec{w_2}, ... , \vec{w_k}}$가 있고, 각 벡터들은 서로 수직이라고 하자. 모든 벡터는 $\vec{0}$이 아니다. 이건 선형독립일까? 만약 선형독립이라면 다음 수식에서 모든 계수가 0이어야 한다.

$$
c_1\vec{w_1} + c_2\vec{w_2} + ... + c_k\vec{w_k} = \vec{0}
$$

집합에 들어있는 임의의 벡터 $\vec{w_i}$를 내적해보자. 벡터들은 서로 수직이니까 $\vec{w_i} \cdot \vec{w_j} = 0$이다. 따라서 수식은 다음과 같이 정리된다.

$$
\vec{w_i} \cdot (c_1\vec{w_1} + c_2\vec{w_2} + ... + c_k\vec{w_k}) = \vec{w_i} \cdot \vec{0} \implies c_i (\vec{w_i} \cdot \vec{w_i}) = 0
$$

$\vec{w_i} \cdot \vec{w_i} \neq 0$ 이므로 $c_i = 0$이다. 임의의 벡터에 대해서 계수가 0이므로, 서로 수직인 벡터들로 구성된 $\set{\vec{w_1}, \vec{w_2}, ... , \vec{w_k}}$는 선형독립이다.

이러한 벡터집합의 원소들이 부분공간 $S$에 속하고, 집합의 크기가 $\dim S$와 같다고 치자. 그러면 $\set{\vec{w_1}, \vec{w_2}, ... , \vec{w_k}}$는 $S$의 basis일까? 모든 $\vec{w}$들은 $S$에 속하기 때문에 $span(\set{\vec{w_1}, \vec{w_2}, ... , \vec{w_k}}) \subset S$ 이다. 그리고 $\set{\vec{w_1}, \vec{w_2}, ... , \vec{w_k}}$는 선형독립이기 때문에 $span(\set{\vec{w_1}, \vec{w_2}, ... , \vec{w_k}})$의 basis이다. [부분공간의 포함관계와 basis의 포함관계]({% link docs/선형대수/부분공간_basis_포함관계.md %}) 페이지에 따르면 $S$의 basis 중에 $\set{\vec{w_1}, \vec{w_2}, ... , \vec{w_k}}$를 포함하는 경우가 존재한다. 그런데 두 집합의 크기가 $\dim S$로 같다. 따라서 $\set{\vec{w_1}, \vec{w_2}, ... , \vec{w_k}}$는 $S$의 basis이다.
